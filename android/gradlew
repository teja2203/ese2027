#!/bin/sh
GRADLE_APP_NAME=Gradle
GRADLE_USER_HOME="${GRADLE_USER_HOME:-${HOME}/.gradle}"
DIRNAME="$(dirname "$0")"
cd "$DIRNAME"
GRADLE_OPTS="${GRADLE_OPTS:--Xmx2048m}"
exec "${JAVA_HOME}/bin/java" $GRADLE_OPTS -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain "$@"
